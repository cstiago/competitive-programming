#include <bits/stdc++.h>
using namespace std;

void solve() {
    int x, y;
    cin >> x >> y;
    
    cout << (x - y) / 2 << "\n";
    
    return;
}

void io() {
    ios_base::sync_with_stdio(0);
    cin.tie(nullptr);
}

int main() {
    io();
    
    //int t; cin >> t; while(t--)
        solve();
        
    return 0;
}

