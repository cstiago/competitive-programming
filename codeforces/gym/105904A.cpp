#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

void solve() {
    int w, a, b, c;
    cin >> w >> a >> b >> c;
    
    cout << ((w >= a + b + c) ? "S" : "N") << "\n";
    
    return;
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(nullptr);
    
    // ll t; cin >> t; while(t--)
        solve();
        
    return 0;
}
