#include<bits/stdc++.h>
using namespace std;

void solve() {
    int a, b;
    cin >> a >> b;

    cout << a*b/2 << endl;

    return;
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(nullptr);

    // long long t;
    // cin >> t;
    
    // while(t--) {
        solve();
    // }

    return 0;
}
